# Google APIs
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# PDF filling
# NOTE: pinned to 4.3.1 — pypdf 6.10.x has a regression in
# update_page_form_field_values that crashes with
# "'int' object has no attribute 'encode'" when rendering
# filled text-field appearance streams.
pypdf==4.3.1
# Signature overlay on Sign-In Log PDFs
Pillow>=10.0.0
reportlab>=4.0.0

# HTTP (Apps Script upload proxy)
requests>=2.31.0

# Claude API (Vision parsing + future use)
anthropic>=0.25.0

# QuickBooks (invoice integration — Phase 6)
intuit-oauth>=1.2.3
python-quickbooks>=0.9.0
