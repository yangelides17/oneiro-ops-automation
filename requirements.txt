# Google APIs
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# PDF filling
pypdf>=3.17.0

# HTTP (Apps Script upload proxy)
requests>=2.31.0

# Claude API (Vision parsing + future use)
anthropic>=0.25.0

# QuickBooks (invoice integration — Phase 6)
intuit-oauth>=1.2.3
python-quickbooks>=0.9.0
